<tr id="comment-<%= comment.id %>" class="bg-white">
  <td>
    <h3 class="text-sm font-medium"><%= comment.user.name %></h3>
    <p class="text-gray-600"><%= simple_format(comment.content) %></p>
  </td>
  <% if current_user.own?(comment) %>
    <td class="action">
      <ul class="flex justify-end">
        <li class="inline-block">
          <%= link_to comment_path(comment), class: "delete-comment-link btn btn-warning", data: { turbo_method: :delete, turbo_confirm: t('defaults.delete_confirm') }  do %>
            <i>destroy</i>
          <% end %>
        </li>
      </ul>
    </td>
  <% end %>
</tr>